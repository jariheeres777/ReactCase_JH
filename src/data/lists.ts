import { IList } from "../model/interfaces/IList";

const initialLists: IList[] = [
  {
    id: 'default_list',
    color: 'blue',
    name: 'Inbox',
    default: true,
    order: 0,
    active:true
  }
];

export default initialLists;